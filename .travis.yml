language: objective-c
branches:
  only:
  - master
notifications:
  email: false
  
before_install: gem install cocoapods calabash-cucumber

script:
- xctool ONLY_ACTIVE_ARCH=NO -workspace WorkshopTalk.xcworkspace -scheme WorkshopTalk build test clean -sdk iphonesimulator
- xctool ONLY_ACTIVE_ARCH=NO -workspace WorkshopTalk.xcworkspace -scheme WorkshopTalk-cal build -sdk iphonesimulator
- cucumber
